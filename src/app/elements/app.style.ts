import {css} from 'lit-element';

// language=CSS
export const style = css`
    :host {
        color: var(--font-color-theme);
        padding: 0.6em;
        max-height: 100vh;
    }

    .panel {
        display: flex;
        flex-direction: column;
    }
`;
