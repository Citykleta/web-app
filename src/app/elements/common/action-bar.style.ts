import {css} from 'lit-element';

// language=CSS
export const style = css`
    ul {
        list-style: none;
        margin: 0.5em;
        padding: 0;
        display: flex;
        justify-content: center;
    }

    li {
        text-align: center;
        padding: 0.5em;
    }

    citykleta-button-icon {
        --size: 5em;
    }
`;
